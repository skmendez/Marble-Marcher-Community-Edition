//! Marble Marcher CSG renderer on Bevy — see rust/DESIGN.md.
//! M4 replaces this stub with the real app (render material, camera, systems).

fn main() {
    println!(
        "marble-marcher-bevy: app lands in milestone M4 (see rust/MILESTONES.md)"
    );
}
